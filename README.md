# LoupeTest

Android приложение для съемки фотографий с интерактивной лупой. После съемки можно прицелиться на любую точку фотографии и увидеть увеличенную область в отдельном окне.

## 🎯 Функционал

- 📸 Съемка фотографий через камеру устройства
- 🎯 Интерактивное прицеливание с визуальным перекрестием
- 🔍 Увеличенное отображение области (лупа 3×)
- 🔄 Возможность сделать новое фото

### Системные требования

| Параметр | Значение |
|----------|----------|
| **Min SDK** | API 29 (Android 10) |
| **Target SDK** | API 36 |
| **Язык** | Kotlin 2.0.21 |
| **Архитектура** | Single Activity, Jetpack Compose |
| **Gradle** | 8.13.2 |

### Технологический стек

**Основные библиотеки:**
- AndroidX Core KTX 1.17.0
- AndroidX Lifecycle Runtime KTX 2.10.0
- AndroidX Activity Compose 1.12.1
- Jetpack Compose BOM 2024.09.00
- Material Design 3

**CameraX (1.4.0):**
- camera-core
- camera-camera2
- camera-lifecycle
- camera-view

### Архитектура

```
com.example.loupetest/
├── MainActivity.kt              # Главная активность и навигация
└── ui/
    ├── components/
    │   ├── CameraView.kt        # Компонент съемки фото
    │   └── PhotoViewer.kt       # Компонент просмотра с лупой
    └── theme/                   # Тема приложения
```

### Разрешения

- `CAMERA` (обязательно, запрашивается динамически)
- `WRITE_EXTERNAL_STORAGE` / `READ_EXTERNAL_STORAGE` (только для Android ≤ 12)

### Аппаратные требования

- Задняя камера (обязательно)
- Автофокус (опционально)


### Хранение данных

**Путь:** `/data/data/com.example.loupetest/files/`  
**Формат имени:** `yyyy-MM-dd-HH-mm-ss-SSS.jpg`

## 🚀 Сборка и запуск

### Требования

- Android Studio (последняя версия)
- JDK 11+
- Android SDK (API 29-36)

### Команды

```bash
# Сборка debug APK
./gradlew assembleDebug

# Установка на устройство
./gradlew installDebug

# Запуск через ADB
adb shell am start -n com.example.loupetest/.MainActivity
```

## 📊 Производительность

**Потребление ресурсов:**
- Память: 50-150 MB (зависит от разрешения камеры)
- CPU: низкая нагрузка в режиме просмотра
- Батарея: стандартное потребление для камеры


## 📝 Информация о проекте

- **Package:** `com.example.loupetest`
- **Version:** 1.0 (Code: 1)
- **Зависимости:** Управляются через `gradle/libs.versions.toml`

## 🐛 Известные ограничения

- Работает только в портретной ориентации
- Нет поддержки жестов масштабирования
- Фотографии сохраняются только во внутреннее хранилище
- Нет истории снятых фотографий

